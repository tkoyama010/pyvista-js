{
  "private": true,
  "scripts": {
    "build": "esbuild ts/pyvista-renderer.ts --bundle --format=iife --outfile=src/pyvista_js/templates/pyvista-renderer.js --target=es2017",
    "lint": "eslint ts/",
    "typecheck": "tsc --noEmit -p ts/tsconfig.json",
    "watch": "esbuild ts/pyvista-renderer.ts --bundle --format=iife --outfile=src/pyvista_js/templates/pyvista-renderer.js --target=es2017 --watch"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.25",
    "eslint": "^10.1.0",
    "typescript": "^5.8",
    "typescript-eslint": "^8.57.2"
  }
}
